{
  "name": "holistic-workspace",
  "private": true,
  "type": "module",
  "comment": "Repo-root pnpm workspace. Spans the project-neutral SDK (holisdk/*) and the Holistic dashboard (frontend/app). holisdk is a top-level sibling — not part of the dashboard — and can be split into its own repo later.",
  "scripts": {
    "dev": "pnpm --filter @holistic/app dev",
    "build": "pnpm --filter @holistic/app build",
    "lint:services": "pnpm --filter @holistic/app lint:services",
    "typecheck:holisdk": "tsc -p holisdk/ui/tsconfig.json"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.8.0",
    "eslint": "^8.57.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
