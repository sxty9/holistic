# Secrets / credentials — never commit
.smbcreds*
*.env
.env*
passdb.tdb
secrets.tdb

# Frontend build artifacts
node_modules/
dist/
*.local

# External service-UI plugins linked in at install time (e.g. hostek)
frontend/external/

# The project-neutral SDK (holisdk) lives OUTSIDE this repo as a sibling and is linked in
# here at setup time (a symlink), exactly like the service-UI plugins above.
/holisdk

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local tooling
.claude/
.sxgate/

# Packaging / build artifacts (regenerable)
*.egg-info/
services/dashboard/build/

# OS / editor junk
.DS_Store
*.swp
*~
